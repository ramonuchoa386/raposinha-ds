import {
    theme,
    invertTheme,
    colorPalette,
    typography,
    breakpoints,
    GlobalStyles,
} from './theme';

import {
    type ColorPalette,
    type ThemeTypography,
    type ThemeBreakpoints,
    type CustomTheme,
    type GlobalStylesProps,
} from './theme';

import { Button, Input } from './components';

export {
    theme,
    invertTheme,
    colorPalette,
    typography,
    breakpoints,
    GlobalStyles,
    Button,
    Input,
};
export type {
    ColorPalette,
    ThemeTypography,
    ThemeBreakpoints,
    CustomTheme,
    GlobalStylesProps,
};
