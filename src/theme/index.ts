import theme from './config';
import { colorPalette, typography, invertTheme, breakpoints } from './config';
import {
    type ColorPalette,
    type ThemeTypography,
    type ThemeBreakpoints,
    type CustomTheme,
} from './config';

import GlobalStyles from './global';
import { type GlobalStylesProps } from './global';

export {
    theme,
    invertTheme,
    colorPalette,
    typography,
    breakpoints,
    GlobalStyles,
};
export type {
    ColorPalette,
    ThemeTypography,
    ThemeBreakpoints,
    CustomTheme,
    GlobalStylesProps,
};
